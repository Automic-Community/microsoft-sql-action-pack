*** Settings ***
Documentation     NOTE:
...               add note here
Suite Setup       Setup_suite
Force Tags        windows
Test Template     Drop Login
Resource          ../../resources/keywords.txt
Resource          ../../resources/context.txt

*** Test Cases ***    OK_NOK    Username                   Password                   JDBC Connection String     JDBC Driver JAR URI    JDBC Driver Class    Connection Interval    No of Connection Retries    Login Name        Fail if not exists    RETCODE    STATUS
1. Fail with invalid username
                      NOK       ${_DB_USERNAME}_invalid    ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST              NO                    0          ${ENDED_NOK}

2. Fail with invalid password
                      NOK       ${_DB_USERNAME}            ${_DB_PASSWORD}_invalid    ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST              NO                    0          ${ENDED_NOK}

3. Fail with invalid jdbc connection string
                      NOK       ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}_invalid    ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST              NO                    0          ${ENDED_NOK}

4. Fail with invalid JDBC Driver Jar URI
                      NOK       ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}_invalid        ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST              NO                    0          ${ENDED_NOK}

5. Fail with invalid JDBC Driver Class
                      NOK       ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}_invalid    ${_INTRVL}             ${_RETRY}                   TEST              NO                    0          ${ENDED_NOK}

6. Fail with case LOGIN is not exist with Fail_If_Exists = YES
                      NOK       ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST_NOT_EXIST    YES                   0          ${ENDED_NOK}

*** Keywords ***
Setup_suite
    [Documentation]    TODO: Write all steps need prepare before testing here
    Log    Start testing for Drop Login Template
