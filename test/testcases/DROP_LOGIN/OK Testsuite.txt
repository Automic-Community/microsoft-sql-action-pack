*** Settings ***
Documentation     NOTE:
...               add note here
Suite Setup       Setup_suite
Force Tags        windows
Test Template     Drop Login
Resource          ../../resources/keywords.txt
Resource          ../../resources/context.txt

*** Test Cases ***    OK_NOK                                   Username           Password           JDBC Connection String    JDBC Driver JAR URI    JDBC Driver Class    Connection Interval    No of Connection Retries    Login to Drop     Fail if not exists    RETCODE      STATUS
1. Success with normal case
                      [Setup]                                  Create Login       OK                 TEST                      ${_DB_USERNAME}        ${_DB_PASSWORD}      ${_CONN_STRING}        ${_JAR}                     ${_CLASS}         ${_INTRVL}            ${_RETRY}    ${_DB_NAME}    TEST_LOGIN1    NO    TEST_LOGIN1    NO    NO    NO    NO    0    ${ENDED_OK}
                      # Require Preparing Login TEST_LOGIN1
                      OK                                       ${_DB_USERNAME}    ${_DB_PASSWORD}    ${_CONN_STRING}           ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST_LOGIN1       NO                    0            ${ENDED_OK}

2. Success with case LOGIN is not exist with Fail_If_Not_Exists = NO
                      OK                                       ${_DB_USERNAME}    ${_DB_PASSWORD}    ${_CONN_STRING}           ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   TEST_NOT_EXIST    NO                    0            ${ENDED_OK}

*** Keywords ***
Setup_suite
    [Documentation]    TODO: Write all steps need prepare before testing here
    Log    Start testing for Drop Login Template
