*** Settings ***
Documentation     NOTE:
...               add note here
Suite Setup       Setup_suite
Force Tags        windows
Test Template     Create DB User
Resource          ../../resources/keywords.txt
Resource          ../../resources/context.txt

*** Test Cases ***    FLAG     Username                   Password                   JDBC Connection String     JDBC Driver JAR URI    JDBC Driver Class    Connection Interval    No of Connection Retries    DB Name                Name of User to Create    Map User to Login Name    Default Schema    Fail If Exists    RETCODE    STATUS
1. Fail with invalid username
                      TEST     ${_DB_USERNAME}_invalid    ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            TEST_CREATE_DB_USER       TEST                      ${EMPTY}          NO                0          ${ENDED_NOK}

2. Fail with invalid password
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}_invalid    ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            TEST_CREATE_DB_USER       TEST                      ${EMPTY}          NO                0          ${ENDED_NOK}

3. Fail with invalid jdbc connection string
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}_invalid    ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            TEST_CREATE_DB_USER       TEST                      ${EMPTY}          NO                0          ${ENDED_NOK}

4. Fail with invalid JDBC Driver Jar URI
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}_invalid        ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            TEST_CREATE_DB_USER       TEST                      ${EMPTY}          NO                0          ${ENDED_NOK}

5. Fail with invalid JDBC Driver Class
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}_invalid    ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            TEST_CREATE_DB_USER       TEST                      ${EMPTY}          NO                0          ${ENDED_NOK}

6. Fail with invalid DB Name and Fail If Exists = YES
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}_invalid    TEST_CREATE_DB_USER       TEST                      ${EMPTY}          YES               0          ${ENDED_NOK}

7. Fail with invalid Login Name
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            TEST_CREATE_DB_USER       _invalid                  ${EMPTY}          NO                0          ${ENDED_NOK}

8. Fail with case DB User is existed and Fail If Exists = YES
                      TEST     ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            ${_DB_USERNAME}           TEST                      ${EMPTY}          YES               1          ${ENDED_NOK}
                      FINAL    ${_DB_USERNAME}            ${_DB_PASSWORD}            ${_CONN_STRING}            ${_JAR}                ${_CLASS}            ${_INTRVL}             ${_RETRY}                   ${_DB_NAME}            ${_DB_USERNAME}           TEST                      ${EMPTY}          YES               0          ${ENDED_NOK}

*** Keywords ***
Create DB User
    [Arguments]    ${FLAG}    ${username}    ${password}    ${jdbc_conn}    ${jdbc_jar}    ${jdbc_class}
    ...    ${interval}    ${retries}    ${db_name}    ${user_name_to_create}    ${db_login_name}    ${default_schema}
    ...    ${fail_if_exists}    ${returnCode}    ${returnStatus}
    ${_CURRENT_TIMESTAMP_}    Get Time    epoch
    Run Keyword If    '${FLAG}' == 'TEST'    Set Suite Variable    ${user_with_timestamp}    ${user_name_to_create}${_CURRENT_TIMESTAMP_}
    Init Action    ${ACTION_CREATE_DB_USER}
    Action Set    ${PRT_CREATE_DB_USER_USERNAME}    ${username}
    Action Set    ${PRT_CREATE_DB_USER_PASSWORD}    ${password}
    Action Set    ${PRT_CREATE_DB_USER_JDBC_CONN}    ${jdbc_conn}
    Action Set    ${PRT_CREATE_DB_USER_DRIVER_JAR}    ${jdbc_jar}
    Action Set    ${PRT_CREATE_DB_USER_DRIVER_CLASS}    ${jdbc_class}
    Action Set    ${PRT_CREATE_DB_USER_CONN_INTRVL}    ${interval}
    Action Set    ${PRT_CREATE_DB_USER_CONN_RETRY}    ${retries}
    Action Set    ${PRT_CREATE_DB_USER_DATABASE_NAME}    ${db_name}
    Action Set    ${PRT_CREATE_DB_USER_USER_NAME_TO_CREATE}    ${user_with_timestamp}
    Action Set    ${PRT_CREATE_DB_USER_DB_LOGIN_NAME}    ${db_login_name}
    Action Set    ${PRT_CREATE_DB_USER_DEFAULT_SCHEMA}    ${default_schema}
    Action Set    ${PRT_CREATE_DB_USER_FAIL_IF_EXISTS}    ${fail_if_exists}
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Not Equal    ${returnCode}
    Action Return Status Should Be    ${returnStatus}
    Set suite variable    ${user_with_timestamp}    ${user_name_to_create}${_CURRENT_TIMESTAMP_}

Setup_suite
    [Documentation]    TODO: Write all steps need prepare before testing here
    Log    Start testing for Create DB User Template
